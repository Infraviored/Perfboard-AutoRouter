There still is a bug in the optimization step, so while Compact is running. Namely even if we already had complete routing I expect that we only render these PCBs that also have complete routing In the renderer we never go down in The completeness of routing The final design or current best design is already correct. So this is only showing these steps where the compactness increased However, the live PCB canvas should also never show a wire that has less completion In that main central view we must allow to have less compact views To visualize the AI thinking but we should limit ourselves to only these configurations That are non-decreasing in routing completion